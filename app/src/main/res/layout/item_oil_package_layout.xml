<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp">

        <TextView
            android:id="@+id/oil_pack_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/colorPrimary"
            android:textSize="13sp"
            android:padding="5dp"
            android:layout_marginStart="10dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:fontFamily="@font/gordita_regular"
            tools:text="Est. $50.00" />

        <TextView
            android:id="@+id/oil_pack_type"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="@android:color/black"
            android:textSize="13sp"
            android:padding="5dp"
            app:layout_constraintStart_toEndOf="@id/oil_pack_price"
            app:layout_constraintEnd_toStartOf="@id/oil_pack_tick"
            android:layout_marginStart="7dp"
            app:layout_constraintTop_toTopOf="@id/oil_pack_price"
            android:fontFamily="@font/gordita_regular"
            tools:text="Blended" />

        <ImageView
            android:id="@+id/oil_pack_tick"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_untick"
            android:padding="5dp"
            app:layout_constraintBottom_toBottomOf="@id/oil_pack_price"
            app:layout_constraintEnd_toEndOf="parent"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>